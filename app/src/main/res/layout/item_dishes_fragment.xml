<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/cardImageView"
        android:layout_width="109dp"
        android:layout_height="109dp"
        app:cardBackgroundColor="@color/backgroundItem"
        app:cardCornerRadius="@dimen/defaultCornerRadius"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/imageView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:contentDescription="@string/dishes_picture_desc"
            tools:src="@drawable/dishes_rice" />

    </com.google.android.material.card.MaterialCardView>

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/title"
        style="@style/ItemDishesFragmentTextStyle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:textAlignment="viewStart"
        app:layout_constraintEnd_toEndOf="@id/cardImageView"
        app:layout_constraintStart_toStartOf="@id/cardImageView"
        app:layout_constraintTop_toBottomOf="@id/cardImageView"
        tools:text="Салат по восточному" />

</androidx.constraintlayout.widget.ConstraintLayout>